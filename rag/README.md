# 🚀 RAG Pattern Extractor — Инструкция для Егора

## Что это?

Вместо ручного заполнения таблицы ты используешь этот инструмент:
1. Находишь паттерн "Коробка" на TradingView
2. Вводишь одну команду в терминал
3. Система автоматически извлекает ВСЕ индикаторы
4. Результат сохраняется в JSON файл

---

## 📦 Установка (один раз)

### 1. Установи Homebrew (если нет)
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### 2. Установи Git и Git LFS
```bash
brew install git git-lfs
git lfs install
```

### 3. Установи Python 3.11
```bash
brew install python@3.11
```

### 4. Клонируй репозиторий
```bash
cd ~
git clone https://github.com/AntI-labs1/ScArlet-Sails.git
cd ScArlet-Sails
```

### 5. Установи зависимости
```bash
pip3 install -r rag/requirements.txt
```

### 6. Проверь что всё работает
```bash
python3 -m rag.cli --help
```

Должен появиться баннер и справка.

---

## 🔄 Ежедневный Workflow

### Шаг 1: Обнови данные (каждое утро)
```bash
cd ~/ScArlet-Sails
git pull
```

### Шаг 2: Найди паттерн на TradingView
- Открой график (например BTC/USDT 1h)
- Найди "Коробку" (боковик с касаниями support/resistance)
- Запиши время свечи ПРОБИТИЯ: `2024-11-26 14:00`

### Шаг 3: Извлеки данные
```bash
python3 -m rag.cli BTC 1h "2024-11-26 14:00"
```

Система покажет:
- Box metrics (support, resistance, касания)
- Индикаторы ДО пробития (RSI, MACD, ATR, Volume)
- W_box score
- Путь к сохранённому файлу

### Шаг 4: Отправь в GitHub (вечером)
```bash
git add rag/patterns/
git commit -m "Паттерны за $(date +%Y-%m-%d)"
git push
```

---

## 📝 Примеры команд

### Базовая команда
```bash
python3 -m rag.cli BTC 1h "2024-11-26 14:00" --direction long
```

### С указанием направления (short | long)
```bash
python3 -m rag.cli ETH 15m "2024-11-26 09:30" --direction short
```

### С заметкой
```bash
python3 -m rag.cli SOL 4h "2024-11-25 20:00" --notes "Сильный уровень, 5 касаний"
```

### Изменить lookback (по умолчанию 48 баров)
```bash
python3 -m rag.cli ENA 1h "2024-11-26 10:00" --lookback 72
```

### Посмотреть все паттерны
```bash
python3 -m rag.cli --list
```

### Посмотреть статистику
```bash
python3 -m rag.cli --stats
```

---

## 📊 Доступные монеты и таймфреймы

### Монеты (14 штук)
```
ALGO  AVAX  BTC  DOT  ENA  ETH  HBAR
LDO   LINK  LTC  ONDO SOL  SUI  UNI
```

### Таймфреймы (4 штуки)
```
15m   1h   4h   1d
```

---

## 🎯 Что извлекается автоматически

| Группа | Что |
|--------|-----|
| Box | support, resistance, touches, duration |
| Price | OHLCV свечи пробития и setup |
| RSI | z-score, зоны (low/mid/high) |
| MACD | z-score |
| ATR | z-score |
| Volume | z-score |
| Trend | up/down/neutral |
| Volatility | low/normal/high |
| Divergence | RSI bullish/bearish |
| Session | hour, asian/european/american |
| **W_box** | **Итоговый score качества** |

---

## 🚨 Ошибки и решения

### "Файл данных не найден"
```bash
git pull   # Обнови репозиторий
```

### "Бар не найден"
- Проверь формат времени: `"YYYY-MM-DD HH:MM"`
- Время должно быть UTC (не локальное!)
- Проверь что свеча существует в данных

### "Недостаточно данных"
- Паттерн слишком близко к началу данных
- Попробуй более поздний timestamp

### "ModuleNotFoundError: rag"
```bash
cd ~/ScArlet-Sails   # Убедись что ты в папке проекта
python3 -m rag.cli ...   # Используй -m
```

---

## 📁 Где хранятся паттерны

```
ScArlet-Sails/
└── rag/
    └── patterns/
        ├── BTC_1h_20241126_1400.json      ← Метаданные
               └── snapshots/
                └── BTC_1h_20241126_1400.csv   ← История (150 баров)
```

Каждый файл содержит:
- Мета-данные (монета, таймфрейм, тип)
- Box metrics
- Все индикаторы
- W_box score

---

## ❓ Вопросы

Пиши в чат проекта или STAR_ANT напрямую.

---

*Scarlet Sails Team — November 2025*
